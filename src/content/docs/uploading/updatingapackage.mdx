---
title: How to update a package
description: The full steps for updating a package in the apt-swarm repos
---
import { Steps } from '@astrojs/starlight/components';

The update steps are alot easier then getting in the repos!
<Steps>
    1. First, move any changes to the packaging repo, this is the repo on the apt-swarm org. This must include a version bump to the packaging.
    2. Create a release, and allow the auto action build the package.
</Steps>